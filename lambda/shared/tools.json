{
  "tools": [
    {
      "name": "move_north",
      "description": "Move one tile north (decreasing Y coordinate)",
      "parameters": {
        "type": "object",
        "properties": {
          "experimentId": {
            "type": "integer",
            "description": "The experiment ID"
          },
          "reasoning": {
            "type": "string",
            "description": "Why you are making this move"
          }
        },
        "required": ["experimentId"]
      }
    },
    {
      "name": "move_south",
      "description": "Move one tile south (increasing Y coordinate)",
      "parameters": {
        "type": "object",
        "properties": {
          "experimentId": {
            "type": "integer",
            "description": "The experiment ID"
          },
          "reasoning": {
            "type": "string",
            "description": "Why you are making this move"
          }
        },
        "required": ["experimentId"]
      }
    },
    {
      "name": "move_east",
      "description": "Move one tile east (increasing X coordinate)",
      "parameters": {
        "type": "object",
        "properties": {
          "experimentId": {
            "type": "integer",
            "description": "The experiment ID"
          },
          "reasoning": {
            "type": "string",
            "description": "Why you are making this move"
          }
        },
        "required": ["experimentId"]
      }
    },
    {
      "name": "move_west",
      "description": "Move one tile west (decreasing X coordinate)",
      "parameters": {
        "type": "object",
        "properties": {
          "experimentId": {
            "type": "integer",
            "description": "The experiment ID"
          },
          "reasoning": {
            "type": "string",
            "description": "Why you are making this move"
          }
        },
        "required": ["experimentId"]
      }
    },
    {
      "name": "recall_last_25",
      "description": "Quick memory refresh. Returns tiles seen in your last 25 actions. Low context cost - good for checking recent exploration without overwhelming your thinking space.",
      "parameters": {
        "type": "object",
        "properties": {
          "experimentId": {
            "type": "integer",
            "description": "The experiment ID"
          },
          "reasoning": {
            "type": "string",
            "description": "Why you need this memory refresh (helps you think through your strategy)"
          }
        },
        "required": ["experimentId"]
      }
    },
    {
      "name": "recall_last_50",
      "description": "Balanced memory. Returns tiles seen in your last 50 actions. Medium context cost - good general-purpose recall for building spatial awareness.",
      "parameters": {
        "type": "object",
        "properties": {
          "experimentId": {
            "type": "integer",
            "description": "The experiment ID"
          },
          "reasoning": {
            "type": "string",
            "description": "Why you need this memory refresh (helps you think through your strategy)"
          }
        },
        "required": ["experimentId"]
      }
    },
    {
      "name": "recall_last_100",
      "description": "Deep memory. Returns tiles seen in your last 100 actions. Higher context cost - good for building comprehensive spatial maps when you need more historical data.",
      "parameters": {
        "type": "object",
        "properties": {
          "experimentId": {
            "type": "integer",
            "description": "The experiment ID"
          },
          "reasoning": {
            "type": "string",
            "description": "Why you need this memory refresh (helps you think through your strategy)"
          }
        },
        "required": ["experimentId"]
      }
    },
    {
      "name": "recall_last_200",
      "description": "Maximum memory. Returns tiles seen in your last 200 actions. High context cost - use sparingly when you absolutely need the full picture of your extensive exploration.",
      "parameters": {
        "type": "object",
        "properties": {
          "experimentId": {
            "type": "integer",
            "description": "The experiment ID"
          },
          "reasoning": {
            "type": "string",
            "description": "Why you need this memory refresh (helps you think through your strategy)"
          }
        },
        "required": ["experimentId"]
      }
    }
  ]
}
