{
  "tools": [
    {
      "name": "move_north",
      "description": "Move one tile north (decreasing Y coordinate)",
      "parameters": {
        "type": "object",
        "properties": {
          "experimentId": {
            "type": "integer",
            "description": "The experiment ID"
          },
          "reasoning": {
            "type": "string",
            "description": "Why you are making this move"
          }
        },
        "required": ["experimentId"]
      }
    },
    {
      "name": "move_south",
      "description": "Move one tile south (increasing Y coordinate)",
      "parameters": {
        "type": "object",
        "properties": {
          "experimentId": {
            "type": "integer",
            "description": "The experiment ID"
          },
          "reasoning": {
            "type": "string",
            "description": "Why you are making this move"
          }
        },
        "required": ["experimentId"]
      }
    },
    {
      "name": "move_east",
      "description": "Move one tile east (increasing X coordinate)",
      "parameters": {
        "type": "object",
        "properties": {
          "experimentId": {
            "type": "integer",
            "description": "The experiment ID"
          },
          "reasoning": {
            "type": "string",
            "description": "Why you are making this move"
          }
        },
        "required": ["experimentId"]
      }
    },
    {
      "name": "move_west",
      "description": "Move one tile west (decreasing X coordinate)",
      "parameters": {
        "type": "object",
        "properties": {
          "experimentId": {
            "type": "integer",
            "description": "The experiment ID"
          },
          "reasoning": {
            "type": "string",
            "description": "Why you are making this move"
          }
        },
        "required": ["experimentId"]
      }
    },
    {
      "name": "recall_movement_history",
      "description": "Retrieve your exploration history including movement path and vision data. Returns chronological list of your actions with success/failure status and tiles seen at each position. Use this to build a mental map and avoid repeating failed moves. Depth options: 25 (recent), 50 (balanced), 100 (deep), 200 (complete).",
      "parameters": {
        "type": "object",
        "properties": {
          "experimentId": {
            "type": "integer",
            "description": "The experiment ID"
          },
          "depth": {
            "type": "integer",
            "description": "How many recent actions to recall (any positive integer). Common choices: 25 (recent), 50 (balanced), 100 (deep), 200 (comprehensive)",
            "minimum": 1
          },
          "reasoning": {
            "type": "string",
            "description": "Why you need this history (helps clarify your strategy)"
          }
        },
        "required": ["experimentId", "depth"]
      }
    }
  ]
}
